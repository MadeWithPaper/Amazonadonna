language: android
sudo: true

env:
   global:
   - ANDROID_API=28
   - EMULATOR_API=28
   - ANDROID_BUILD_TOOLS=28.0.3

android:
   components:
   # The BuildTools version used by project
   - tools
   - platform-tools
   - build-tools-$ANDROID_BUILD_TOOLS
   - extra
   - addon-google_apis-google-$ANDROID_API
   # The SDK version used to complie project
   - android-$ANDROID_API
   # needed to run emulator for ui tests
   - android-$EMULATOR_API_LEVEL
   - sys-img-armeabi-v7a-addon-google_apis-google-$ANDROID_API_LEVEL
   - sys-img-armeabi-v7a-addon-google_apis-google-$EMULATOR_API_LEVEL

licenses:
   - android-sdk-preview-license-.+
   - android-sdk-license-.+
   - google-gdk-license-.+

jdk:
   - oraclejdk8

# Emulator Management: Create, Start and Wait
#before_script:
#   - android -v create avd --force --name "test android-28" --abi armeabi-v7a
#   - emulator -avd test -no-skin -no-audio -no-window &
#   - android-wait-for-emulator
#   - adb shell input keyevent 82 &

script:
   - ./gradlew clea build checkn
