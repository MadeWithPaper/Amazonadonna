language: node_js
node_js:
  - 'node'

services:
  - docker
before_install:
  - docker pull amazon/dynamodb-local
  - docker run -d -p 8000:8000 amazon/dynamodb-local

before_script:
  - cd API
  - npm i

script:
  - npm test
